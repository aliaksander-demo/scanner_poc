name: scanner
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cunning_document_scanner: ^1.2.3
  google_mlkit_text_recognition: ^0.14.0
  pdfx: ^2.8.0

  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  navigation:
    path: ../../navigation

dev_dependencies:
  lints: ^2.1.0
  auto_route_generator: ^9.0.0
  build_runner: ^2.4.8
